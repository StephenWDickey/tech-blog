



{{#if loggedIn}} 
 
  {{#each posts as |post|}}
  <div class="p-5 mx-5">
   <h1 class="text-light border-bottom border-light pt-5">Showing Your Posts</h1>
   <ol class="mt-3 post-list border border-dark bg-light"> 
    <li>
      <article class="user-post">
        <div class="title">
          <a href="{{post.post_url}}" class="text-dark fw-bold post-title" target="_blank">{{post.title}}</a>
          <span>({{post.post_url}})</span>
        </div>
        <div class="meta">
          {{post.vote_count}} point(s) by {{post.user.username}} on
          {{post.created_at}}
        |
          <a href="/post/{{post.id}}" class="text-dark fw-bold comments-length">{{post.comments.length}} comment(s)</a>
        </div>
      </article>
    </li>
  </ol>
  {{else}}
    <h1 class="pt-5 px-5 text-light border-bottom border-light">Looks like you haven't posted anything yet!</h1>
    
  {{/each}}

{{else}}
<div class="p-5">
  <h1 class="text-light pt-5 px-5 border-bottom border-light">You need to login or sign-up to view posts!</h1>
  <a href="/login" class="mx-5 mt-3 btn btn-primary btn-lg login">Login!</a>
</div>
{{/if}}


{{#if loggedIn}}
<script src="/javascript/logout.js"></script>


{{/if}}
